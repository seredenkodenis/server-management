server.port=8080
spring.thymeleaf.cache=false
spring.jpa.database=postgresql
spring.datasource.url = jdbc:postgresql://localhost:5432/server
spring.datasource.username = postgres
spring.jpa.show-sql = true
spring.jpa.hibernate.ddl-auto = create-drop
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.PostgreSQLDialect


spring.mail.host=mail.denis-seredenko.com
spring.mail.port=587
spring.mail.username=admin@denis-seredenko.com
spring.mail.password=seredenko302003
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

#same as spring.mail.username
mailFrom=admin@denis-seredenko.com

#where you will recieve info
ownEmail=denis-seredenko@ukr.net

#add your own cron job for processes(this code will check every minute)
cronProc=0 * * * * *

#add cron job for services(this code will check every minute)
cronService=0 * * * * *

#full path to resources/script
pathToScripts=/home/denis/IdeaProjects/server-management/src/main/resources/scripts/

#full path to recources/results/
pathToResults=/home/denis/IdeaProjects/server-management/src/main/resources/results
#full path to recources
pathToResources=/home/denis/IdeaProjects/server-management/src/main/resources/

#sudo password | we don't collect your password, we need it to work with services
sudoPass=seredenko302003

spring.jpa.open-in-view=true